---version: beta-v3name: drag-to-delete-crumple-filename_zh: "拖拽删除文件动效"cover_video: "../assets/drag-to-delete-crumple-file.mp4"cdn_video_url: "https://pub-78bb53484bcd4179b692b8ebeee0e014.r2.dev/drag-to-delete-crumple-file.mp4"tags: ["Drag", "Drop", "Delete", "File"]preview: { backgroundColor: "#FFFFFF", textColor: "#171717" }description: >  An interactive delete mechanism where a file can be dragged and dropped into a trash can. If missed, the file crumples into a ball, bounces, and returns to its original position. If successfully dropped, the file is 'deleted' with a subtle trash can animation and a confirmation toast.website: ""rendering_engine: "DOM_CSS"assets:  required: true  items:    - name: "File Icon"      type: "SVG / PNG"      description: "The initial file icon for dragging."    - name: "Trash Can Icon"      type: "SVG / PNG"      description: "The target icon for deletion."    - name: "Crumpled Paper Icon"      type: "SVG / PNG"      description: "Icon displayed when a file is missed and crumples."  dependencies:    - "framer-motion@^11.0.0"motion_tokens:  selected_preset: "PRESET_SPRING_BOUNCE"  transform_origin: "center center"  stagger_delay: "0ms"  active_physics:    stiffness: 200    damping: 25    mass: 1  css_fallback_easing: "cubic-bezier(0.16, 1, 0.3, 1)"  duration: "350ms"  variants:    initial: { opacity: 1, scale: 1 }    animate: { opacity: 1, scale: 1.05 }    exit: { opacity: 0, scale: 0.95 }---# 拖拽删除文件动效 / Drag-to-Delete Crumple File Specification & Implementation Protocol## 0. Prerequisite & Guardrail (防降级校验)> **⚠️ 核心编码规则：**> 1. **单体原子化原则**：仅导出 1 个高度可复用的原子 UI 组件（如 `<MotionButton />`），业务属性暴露为 `props`。> 2. **轻量通用性**：不依赖外部 3D 资源库，保证开箱即用。## 1. Interaction & Feel Vibe (动效体感)- **Visual Physics Class**: Fluid-Elastic / Snappy-Mechanical- **Core Experience**: Interactive file deletion with drag-and-drop mechanics. Successful deletion involves the file smoothly entering the trash can, while a missed drop results in the file crumpling, bouncing, and returning.- **Interaction Flow**: Drag file -> Drop into trash can (Success) / Drop outside (Miss). Success: File disappears into trash, trash can animates, toast appears. Miss: File transforms to crumpled paper, bounces, returns, miss counter increments.## 2. Component DOM Mapping (原子组件结构映射)- **[Stage Container]** (`div` - Outer card/dialog)- **[Draggable File Wrapper]** (`motion.div` - Wrapper for the file icon, handles drag logic)- **[File Icon / Crumpled Icon]** (`img` or `svg` - Conditionally rendered based on `isCrumpled`)- **[Trash Can Container]** (`motion.div` - For the trash can icon, handles gulp animation)- **[Trash Can Icon]** (`img` or `svg`)- **[Toast Notification]** (`motion.div` - For delete confirmation)## 3. Detailed Timeline Sequence (时序编排)- **[0ms - onDragStart] Trigger Phase**: Draggable file scales slightly up. `isDragging` state becomes true.- **[onDragEnd: Hit] Success Phase**: File rapidly scales down to 0 and fades out as it enters the trash can. The trash can performs a quick `scale` animation (`[1, 1.1, 1]`). A 